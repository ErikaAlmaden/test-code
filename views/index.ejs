<h1 class="mb-3">Welcome<% if (user && user.name) { %>, <%= user.name %><% } %>!</h1>
<p class="text-muted">This is a simple app to track your books.</p>
<% if (!user) { %>

<% } else { %>
<a href="/books" class="btn btn-success">Go to your books</a>
<% } %>